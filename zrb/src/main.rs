mod backup_manager;
mod compression;
mod config;
mod encryption;
mod hash;
mod remote;
mod snapshot;
mod status;

use crate::snapshot::*;
use crate::status::model::*;

fn main() {
    let a: Hash = vec![];
    println!("Hello, world!");
}
