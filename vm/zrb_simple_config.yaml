base_dir: /tmp/zrb_backups/
age_public_key: age1ed9kad8ddqpjnt7k50tmxu8ngxte5ghmwsse7ckt6kdtvc7afpkqlj5ddk
s3:
  enabled: true
  bucket: zfs-backups
  prefix: zfs-backups
  region: us-east-1
  endpoint: "http://127.0.0.1:9000" # point to local MinIO
  storage_class: # Options: STANDARD, INTELLIGENT_TIERING, STANDARD_IA, ONEZONE_IA, GLACIER, DEEP_ARCHIVE, GLACIER_IR
    manifest: STANDARD
    backup_data: [DEEP_ARCHIVE, DEEP_ARCHIVE, GLACIER, GLACIER_IR]
  retry:
    max_attempts: 5
tasks:
  - name: daily_backup
    description: Daily backup task
    pool: testpool
    dataset: backup_data
    enabled: true
  - name: weekly_backup
    description: Weekly backup task
    pool: testpool
    dataset: backup_data
    enabled: false
